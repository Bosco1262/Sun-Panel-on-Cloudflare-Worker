export enum PanelStateNetworkModeEnum {
  // LAN
  // 局域网
  'lan' = 0,
  // Internet
  // 互联网
  'wan' = 1,
}

export enum PanelPanelConfigStyleEnum {
  // Icon style (small icon / square)
  // 图标风格 (小图标/正方形)
  'icon' = 1,
  // Detail style (bar)
  // 详情风格 (长条形)
  'info' = 0,
}

// How search-box results are opened
// 搜索框结果打开方式
export enum SearchEngineOpenMethodEnum {
  // Open in the current page
  // 当前页面打开
  'currentPage' = 0,
  // Open in a new window
  // 新窗口打开
  'newWindow' = 1,
}
